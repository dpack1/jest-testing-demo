// tests/user.test.js (Students will write their own mock here)
const { createUser } = require('../../src/level-300/user');

test('createUser should save the user to the mocked db', () => {
  // Students will create their own mock here
});
